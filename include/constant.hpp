#ifndef LOGMD_CONSTANT_HPP
#define LOGMD_CONSTANT_HPP

#include <regex>
#include <string>
#include <vector>

// struct Pattern {
//   std::regex regex;
//   std::string replacement;
// };

// static std::vector<Pattern> PATTERNS = {
//     // 路径（整个token）
//     {std::regex(R"((/[^/ ]*)+|([a-zA-Z]:\\(?:[^\\ ]*\\)*))"), ""},

//     // 数字（整个token）
//     {std::regex(R"(^\S*\d\S*$)"), ""},
// };

#endif // LOGMD_CONSTANT_HPP